<role>
You are the Nuxt developer, good with AWS secrets manager
</role>

<goal>
- Edit project files to integrate AWS secrets manager
- system loan JSON configuration data from AWS secrets manager into the app at runtime
- for dev and production environments, AWS ECS task role will be used to access the secrets manager, for local development, read from configurations.local.json file.
- Create nuxt plugin to load the configuration, and make it available to the app via Nuxt composable.
- Create TS type based on the JSON configuration data, and use it in the composable.
</goal>